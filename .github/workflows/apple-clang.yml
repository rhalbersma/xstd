#          Copyright Rein Halbersma 2014-2026.
# Distributed under the Boost Software License, Version 1.0.
#    (See accompanying file LICENSE_1_0.txt or copy at
#          http://www.boost.org/LICENSE_1_0.txt)

name: Apple Clang

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow_ref }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  # canary.yml calls this weekly, so the stub stays callable itself.
  workflow_call:

jobs:
  # Two rungs, not three: Apple publishes no Clang trunk, and the shared
  # workflow drops the development rung with a notice rather than failing.
  apple_clang:
    uses: rhalbersma/cpp-ci/.github/workflows/apple-clang.yml@31ad36893e66a2083531e64427fdf734665d8aa1 # v0.7.1
