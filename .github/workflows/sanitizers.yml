#          Copyright Rein Halbersma 2014-2026.
# Distributed under the Boost Software License, Version 1.0.
#    (See accompanying file LICENSE_1_0.txt or copy at
#          http://www.boost.org/LICENSE_1_0.txt)

name: Sanitizers

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # No inputs: this library is C++23 and builds on every stable rung, which is
  # what the shared workflow defaults to. A repository needing a newer compiler
  # names the rung instead of the version, and this one may yet.
  sanitizers:
    uses: rhalbersma/cpp-ci/.github/workflows/sanitizers.yml@294abf16aee51dbea70891b75eb1512f2b396e28 # v0.7.0
